# A — 스테이지 맵 + 랜덤 스포너 설계서

> `WEEKEND_PLAN.md` §3 의 확정판. 브랜치 `feat-stage`.
> 기획서 원본: `Doc/설계/plans/slime-rancher-roguelite.json` (spec-001/004/006/009/012 관련)

---

## 0. 한 줄로

**바이옴 씬 3개는 그대로 두고, 씬이 열릴 때 120×90 타일짜리 방·복도 맵을 코드가 그린다.
맵 안에는 바이옴 구역이 2~3종 섞여 있고, 슬라임은 자기 구역의 종·티어로 뽑혀 방마다 흩어진다.**

> **처음 보는 사람용 설명**
> 지금 게임은 미리 손으로 그려둔 작은 방 하나에서만 논다. 매번 똑같다.
> 앞으로는 게임이 시작될 때마다 컴퓨터가 **레고 조립하듯** 방을 여러 개 만들고 복도로 잇는다.
> 방마다 "여기는 늪지", "여기는 용암지" 하고 성격을 정해서 바닥 그림과 나오는 슬라임이 달라진다.
> 그래서 한 판이 끝나고 다시 들어가면 지도가 통째로 달라져 있다.

---

## 1. 확정 사항 (사용자 결정, 2026-08-08)

| 항목 | 결정 | 주말 계획 대비 |
|---|---|---|
| 맵 구조 | **방 + 복도** (BSP 분할) | 그대로 |
| 낙인 기준 `biomeId` | **플레이어가 밟고 있는 구역** | 신규 — 아래 §3 |
| 진행 방식 | **한 판 큰 맵, 게이트 없음** | ⚠️ `StageGate.cs` **삭제**, `depth` 개념 없음 |
| 한 층 크기 | **120×90 타일** | 신규(계획엔 방 크기만 있었음) |

`depth` 가 사라지면서 난이도를 올리는 축은 **낙인 티어 하나뿐**이 된다.
구역별로 티어가 따로 붙으므로(§3) 한 맵 안에서도 "이 구역은 오염이 심하다"가 성립한다 — 축이 하나여도 밋밋하지 않다.

---

## 2. 실측 근거 (이 설계가 서 있는 바닥)

| 사실 | 근거 |
|---|---|
| `WildSlimeAgent.Initialize(species, tier)` 이미 존재 | `Assets/Scripts/World/WildSlimeAgent.cs:69` |
| → **스포너는 `WildSlimeAgent.cs` 를 한 줄도 안 고친다** | `feat-companion` 과 파일 충돌 0 |
| `WildSlime.prefab` 도 인스펙터 참조로만 사용 | B 소유 프리팹 안 건드림 |
| Grid 자식 = `Ground` `Walls` `Decor` `FogOfWar` | `Biome.unity` (셀 1×1) |
| 늪지만 타일 이름이 `MarshTile_*` (나머지 `Tile_GroundBlend_*`) | `Assets/Resources/Tiles/marsh/` |
| `FogOfWarReveal` 은 **타일이 이미 있는 칸만** 걷어냄 | `FogOfWarReveal.cs:49` (`HasTile`) |
| `com.unity.2d.tilemap.extras 8.0.3` 설치됨 | `Packages/manifest.json` (RuleTile 가능) |
| 화면 = 17.8 × 10 유닛 (ortho 5) | 카메라 |
| 120×90 = 화면 **가로 6.7 · 세로 9** 배. 현재 맵(24×16)의 **28배** | 계산 |

---

## 3. 기획서와의 충돌 3건 — 어떻게 푸는가

### 3-1. spec-009 "바이옴 = 씬 1개, 종 풀 1개" ↔ 섞인 맵

**씬 구조는 안 건드린다.** 다이브는 여전히 `GameManager.StartRun(biomeId)` → 그 바이옴의 씬을 연다.
섞이는 것은 **씬 안의 구역**이다. `Test_Dive_Loads_Selected_Biome` 은 그대로 통과한다.

- 씬의 바이옴 = **홈 바이옴**. 방의 **60% 이상**을 차지한다.
- 나머지 40% 를 외지 바이옴 1~2종이 나눠 갖는다.
- 종 풀은 **방이 속한 구역**의 `BiomeEntry.speciesPool` 에서 뽑는다 → spec-009 의
  "바이옴마다 다른 종이 나온다"는 오히려 더 강해진다.

### 3-2. spec-004 낙인 — "어디서 죽었나"가 애매해짐

**방마다 트리거를 깔고, 들어갈 때 `GameManager.CurrentBiomeId` 를 그 방의 구역 값으로 바꾼다.**

- 늪지 씬 안의 용암 구역에서 죽으면 → **용암지에 낙인** → 다음 런은 용암지 씬에서 시작.
  기획서의 "죽은 땅에서 다시 시작한다"가 그대로 산다.
- 오염 티어도 구역별로 읽힌다: 같은 맵 안에서 용암 구역 슬라임만 세다.

### 3-3. spec-006 오염 유전 — 같은 값을 읽는다

`CorruptedGeneTagger` 는 `CurrentBiomeId` 를 보므로 3-2 로 자동 해결.
**포획한 슬라임의 오염 유전 = 잡은 구역 기준**이 된다 (의도한 결과).

### 3-4. ARCH-024 (spec-009 아키텍처 카드) 준수 여부

| 카드 규칙 | 이 설계 |
|---|---|
| 격자 아래 역할별 타일맵 | ✅ 기존 `Ground`/`Walls`/`Decor`/`FogOfWar` 사용 |
| 벽 겹에만 콜라이더 + Composite | ✅ 기존 배선 유지 |
| 타일마다 오브젝트 배치 금지 | ✅ `SetTilesBlock` |
| 규칙 타일(Rule Tile) 사용 권장 | ⚠️ **일단 위반** — §9 부채 참고 |

---

## 4. 파일 목록

### 신규

| 파일 | 하는 일 | 대략 |
|---|---|---|
| `Scripts/World/StageLayout.cs` | **순수 C#**(Unity 무의존). BSP 분할 → 방/복도/구역/벽밴드/스폰후보 계산 | ~180줄 |
| `Scripts/World/StageMapGenerator.cs` | `StageLayout` 결과를 `SetTilesBlock` 으로 타일맵에 그리고, 방 트리거·추출구·플레이어를 배치 | ~130줄 |
| `Scripts/World/SlimeSpawner.cs` | 방 목록 받아 슬라임 스폰. `Initialize(species, tier)` 로 주입 | ~80줄 |
| `Scripts/World/StageRegionTrigger.cs` | 방에 붙는 트리거. 플레이어 진입 시 `CurrentBiomeId` 갱신 | ~25줄 |
| `Scripts/Data/StagePalette.cs` | SO. 구역 하나의 그림 묶음 + `biomeId`. 최초엔 바닥 1종+벽 8방향 필드였다가 §9-1 에서 `wangBlend[16]`(바닥용) 로 통합 → 벽 콜라이더 버그로 `wangBlendCollidable[16]`(벽 전용, 콜라이더 있음) 추가. `fogLevels[5]` 는 §9-2 에서 색 틴트로 3바이옴 다 채움 | 완료 |
| `Assets/SO/Stages/{Biome,Marsh,Ashfall}Palette.asset` | 위 SO 인스턴스 3개 | 완료 |
| `Assets/Tests/PlayMode/StageLayoutTests.cs` | §8 검증 6종 | ~120줄 |

**`StageLayout` 을 `MonoBehaviour` 로 안 만드는 이유**: Tilemap·GameObject 없이 순수 계산만 하면
테스트에서 씬 없이 바로 돌릴 수 있다. "지도가 이상하다"의 원인이 계산인지 그리기인지 즉시 갈린다.

### 수정

| 파일 | 변경 | 소유 |
|---|---|---|
| `Systems/GameManager.cs` | `SetCurrentBiome(string)` public 메서드 추가. **`CurrentRunSeed` 는 안 만들었다** — 시드는 `StageMapGenerator` 가 직접 굴려서(§5-7) GameManager 가 들고 있을 이유가 없어졌다(안 쓰는 API 는 안 만든다) | A |
| `Assets/Scenes/{Biome,BiomeMarsh,BiomeAshfall}.unity` | 손배치 슬라임 2마리 삭제 · 손배치 타일 삭제 · `StageDirector` 오브젝트 1개 추가 | A |

**B 소유 파일 변경 0건.** `WildSlimeAgent` `SlimeInstance` `PlayerRoster` `WildSlime.prefab` 전부 안 건드린다.

### 삭제된 계획

`StageGate.cs` — 게이트 없음 결정으로 불필요. `GameManager.CurrentStageDepth` 도 안 만든다.

---

## 5. 생성 알고리즘

### 5-1. 방 뽑기 — BSP 재귀 분할

```
전체 120×90 사각형에서 시작
  → 긴 축을 40~60% 지점에서 자른다
  → 조각이 최소 크기(20×17)보다 작아지면 멈춘다
  → 각 조각(leaf) 안쪽에 여백 2~4칸 두고 방을 그린다
```

**시드 200개 실측 (2026-08-08)**: 방 **16~20개** · 방 크기 **12×22 × 10×18** ·
바닥 칸 **4,989~5,801** (전체 10,800 중 46~54%) · 생성 **2.7ms/장**.
조각 최소 크기를 24×20 으로 잡았을 때는 방이 9~16개로 흩어져 20×17 로 내렸다.
- **겹치는 방이 원리적으로 안 나온다** — 자른 조각 안에만 그리므로.
  무작위 배치 + 겹침 검사 방식보다 코드가 짧고 무한루프가 없다.

### 5-2. 복도 — BSP 트리를 그대로 씀

- 분할 트리의 **형제 노드끼리** 중심점을 L자(가로→세로)로 잇는다.
- 트리라서 **모든 방이 반드시 이어진다** — 연결성 보정 코드가 필요 없다.
- 그 위에 **무작위 3개**의 추가 연결을 넣는다(막다른 길만 있는 지도는 되돌아오기가 지겹다).
- 복도 폭 **3칸** (슬라임에 막혀 못 지나가는 일 방지. 2칸이면 슬라임 2마리에 막힌다)

### 5-3. 벽 — 바닥 둘레 3칸만

바닥이 아닌 칸 전부를 벽으로 채우면 10,800칸 중 7,000칸이 벽이 되어
콜라이더 병합 비용이 그만큼 든다. **바닥에서 3칸 이내만 벽 타일을 깐다.**

- 그 바깥은 타일 없음 = 카메라 배경색(검정) = "동굴 밖 어둠". Core Keeper 도 이 모양이다.
- **3칸인 이유**: 카메라가 플레이어 기준 위아래 5유닛을 본다. 플레이어가 벽에 붙으면
  벽 너머가 보이는데, 3칸이면 나머지는 검정이라 어둠으로 읽힌다. 1칸이면 "지도가 잘렸다"로 읽힌다.

### 5-4. 구역(바이옴) 배정 — **칸이 아니라 방 단위**

```
구역 시드 3개를 방 목록에서 무작위로 고른다
  시드 2개 = 홈 바이옴 / 시드 1개 = 외지 바이옴 (BiomeCatalog 에서 홈 제외하고 추첨)
각 방은 "가장 가까운 시드"의 바이옴을 갖는다
홈 비중이 60% 미만이면 → 홈 시드를 하나 더 넣고 다시 배정
복도는 양 끝 방 중 가까운 쪽 팔레트를 쓴다
시작 방(room 0)은 예외 없이 홈 바이옴으로 강제한다
```

**시작 방 강제가 나중에 추가됐다 — 실측으로 잡은 버그다(2026-08-08).** 씨드가 시작 방 근처에
하나도 안 박히면 "홈 씬(예: 초원)인데 플레이어가 시작하자마자 서 있는 바닥이 화산재"가 나왔다.
추출구 방은 그대로 최근접 규칙을 따른다 — 가장 먼 방이 외지여도 이상하지 않다(오히려 "깊이
들어갈수록 낯선 땅" 이라는 의도와 맞는다).

**칸 단위 보로노이를 안 쓰는 이유**: 방 단위면 "이 방은 늪지"가 딱 떨어져서
낙인 트리거(§3-2)가 **방마다 콜라이더 하나**로 끝난다. 칸 단위면 경계에서 `CurrentBiomeId` 가
초당 수십 번 흔들리고, 어느 바이옴에서 죽었는지가 다시 애매해진다.

### 5-5. 배치

| 대상 | 규칙 |
|---|---|
| 플레이어 | **시작 방**(BSP 첫 leaf) 중심 |
| `ExtractionPoint` | 시작 방에서 **복도 그래프 거리가 최대**인 방의 중심 |
| 방 트리거 | 방마다 1개, 방 크기의 `BoxCollider2D isTrigger` |
| 안개(`FogOfWar`) | 바닥 + 벽밴드 전 칸에 최고 농도 타일 |

**추출구를 가장 먼 방에 두는 이유**: 게이트가 없어졌으므로 "얼마나 깊이 갈까"의 긴장을
**"추출구까지 걸어 돌아가는 거리"**가 대신한다. 배낭이 이미 "죽으면 몰수"라 축이 맞는다.

### 5-6. 방 패턴 — 재미를 만드는 자리

**재미는 방 모양이 아니라, 방 모양이 이 게임의 규칙을 건드릴 때 생긴다.** 건드릴 규칙 3개:

| 게임의 성질 | 근거 | 패턴이 쓰는 방식 |
|---|---|---|
| 슬라임 추격은 **직선뿐**(길찾기 없음) | spec-012 `outOfScope` | **기둥** — 시야를 끊어 따돌리기가 성립 |
| 배낭은 **죽으면 몰수** | spec-011 | **둥지** — 더 먹을까 / 지금 뺄까 |
| 위험은 **비교로만 느껴진다** | — | **쉼터** — 조용한 방이 있어야 다음 방이 무섭다 |

> **쉽게**: 슬라임은 벽이 있어도 플레이어 쪽으로 일직선으로만 온다. 방 가운데 기둥을 세워두면
> 플레이어가 기둥 뒤로 돌 때 슬라임이 기둥에 부딪혀 낑낑댄다. 원래 "AI 가 멍청한 것"인데
> 기둥을 놓는 순간 **"머리 쓰면 이긴다"는 재미**가 된다.

#### v1 패턴 3종 (사용자 결정 2026-08-08)

| 패턴 | 모양 | 만드는 결정 | 슬라임 배율 |
|---|---|---|---|
| `Pillar` 기둥 | 2×2 벽덩이 4~8개 흩뿌림 | 싸울까 / 따돌릴까 | ×1 |
| `Safe` 쉼터 | 아무것도 없음 | (없음 — 숨 고르기) | **×0** |
| `Nest` 둥지 | 네모 그대로, **막다른 방에만** | 욕심낼까 / 지나칠까 | **×3, 티어 +1** |

#### 배치 규칙 — 방 모양보다 순서가 중요하다

방을 따로따로 굴리면 둥지 3개가 나란히 나온다. **시작 방에서의 거리로 등급을 매겨 굴린다.**

```
시작 방        → Safe 고정 (첫 3초에 죽으면 안 된다)
가까운 방      → Pillar 60% / Safe 40%
중간 방        → Pillar 70% / Safe 30%
먼 방          → Pillar 60% / Nest 40%
막다른 방      → Nest 가중치 ×3   (안 들어가도 되는 방이 제일 위험해야 한다)
추출구 방      → Pillar 고정      (마지막 관문. 뻥 뚫려 있으면 그냥 걸어 나간다)
전체 보장      → Safe 최소 2개
```

가중치 표는 **인스펙터 배열**로 뺀다 — 밸런싱에 재컴파일이 걸리면 안 된다.

#### 구현 — 새 구조 만들지 않는다

`RoomPattern` **enum 하나 + `switch` 하나**. 인터페이스·SO·팩토리 없음(패턴 쓰는 사람과 코드 쓰는 사람이 같다).

```csharp
// 기둥 4~8개를 놓되, 방 중심 5×5 는 비워 둔다
// → 그 자리에 추출구·플레이어가 설 수 있다
// 다 놓은 뒤 방 안 바닥이 한 덩어리가 아니면 네모로 되돌린다
if (!RoomFullyConnected(room.bounds)) FillFloor(room.bounds);
```

**되돌리기 보험이 이 기능의 핵심이다.** 전체 연결성 테스트(§8-2)는 방 **사이**만 보므로
방 **안**이 끊기는 건 여기서만 잡힌다.

**실측으로 잡은 두 건 (2026-08-08, 시드 100개)**

| 증상 | 원인 | 대응 |
|---|---|---|
| 시드 12/100 에서 **추출구가 벽에 묻힘** | 기둥이 방 중심을 덮었다. 추출구 방은 Pillar 고정이라 늘 노출돼 있었다 | 방 중심 5×5 를 기둥 금지 구역으로 |
| 시드 4/100 에서 **방 안 바닥이 갈라짐** | 문 기준 검사는 통과하는데, 구석에 갇힌 바닥 웅덩이가 남았다. 거기 스폰된 슬라임은 영영 못 잡는다 | 문이 아니라 **방 안 바닥 전부**가 한 덩어리인지 검사 |

두 번째가 특히 조용하다 — 문은 멀쩡히 열려 있어서 눈으로도, 문 검사로도 안 잡힌다.

**기둥은 벽과 같은 wang 코너 블렌드를 그대로 쓴다**(§9-1) — 별도 코드 없이 `IsWall` 데이터를 공유해서
자동으로 해결됐다.

### 5-7. 시드

`StageMapGenerator` 가 `Generate()` 안에서 직접 굴린다(`Environment.TickCount`). **저장하지 않는다** —
런이 끝나면 지도도 끝이다. `useFixedSeed`+`fixedSeed` 인스펙터 값을 켜면 같은 지도를 반복 재현한다
(버그 재현용). **`GameManager` 는 시드를 안 든다** — 아무도 안 읽어서(설계 당시엔 GameManager 소유를
가정했지만 실제로 시드를 쓰는 곳이 생성기 하나뿐이라 옮길 이유가 없었다).

---

## 6. 스포너 규칙

| 항목 | 값 |
|---|---|
| 방당 마리수 | **2~4** × 방 패턴 배율(§5-6: `Safe` 0 / `Pillar` 1 / `Nest` 3) |
| 동시 상한 | **없음** (사용자 결정 2026-08-08). 방 18개 기준 **약 50~60마리** |
| 위치 | 그 방의 바닥 칸 중 무작위. 벽 인접 1칸 제외 |
| 최소 간격 | 슬라임끼리 1.5 유닛, 플레이어에서 10 유닛 |
| 종 | **방이 속한 구역**의 `BiomeEntry.speciesPool` 에서 추첨 |
| 티어 | **방이 속한 구역**의 `BiomeStigmaManager.GetCorruptionTier(구역 biomeId)` + `Nest` 면 +1 |
| 주입 | `Instantiate(prefab)` → `agent.Initialize(species, tier)` |

**티어 배율을 밖에서 다시 곱하지 않는다** — `Initialize` 안에서 이미 `1 + 0.15×tier` 와 종 편향이
순서대로 적용된다. 밖에서 또 걸면 두 번 곱해진다.

**스폰은 생성 직후 한 번에 전부.** 플레이어 근처만 스폰하는 방식은 지금 필요 없다.

**상한을 없앤 대신 조절 손잡이를 남긴다** — 방당 최소·최대 마리수를 인스펙터 값으로 둔다.
54마리 각각이 `FixedUpdate` 에서 거리 계산 1회 + Rigidbody2D 1개다. 에디터에서는 여유롭지만
**WebGL 실측 전에는 안전하다고 말할 수 없다.** 첫 빌드에서 프레임이 떨어지면 코드가 아니라
그 두 숫자를 내린다(`ponytail:` 주석으로 표시). 필요해지면 그때 "먼 방은 나중에 스폰"으로 올라간다.

---

## 7. 씬 배선 변경 (3씬 동일)

```
Grid
 ├ Ground     ← 생성기가 채움 (손배치 타일 전부 삭제)
 ├ Walls      ← 생성기가 채움 (Composite 배선 유지)
 ├ Decor      ← 그대로 (장식은 나중)
 └ FogOfWar   ← 생성기가 채움
StageDirector          ← 신규 빈 오브젝트
 ├ StageMapGenerator   (팔레트 3개 + 타일맵 4개 참조)
 └ SlimeSpawner        (WildSlime 프리팹 참조)
ExtractionPoint        ← 프리팹 인스턴스 유지, 생성기가 위치만 옮김
Player                 ← 인스턴스 유지, 생성기가 위치만 옮김
WildSlime ×2           ← 삭제
```

`ExtractionPoint` 와 `Player` 를 **삭제하지 않고 위치만 옮기는 이유**: 씬 diff 를 최소로 유지해
`.unity` 병합 사고(§8, `f6999ef` 사례) 표면적을 줄인다.

**머지 후 오브젝트 총수 기준선(2026-08-08, 3씬 배선 후 실측): Biome 110 / BiomeMarsh 395 / BiomeAshfall 116** (Hub 67 은 안 건드려서 그대로). 원래 예측은 "슬라임 2개 감소 + `StageDirector` 1개 증가 = 순 -1" 였는데 실측은 **+2** — 예측이 틀렸다:

| 실제 변화 | 블록 수 |
|---|---|
| `WildSlime` 삭제 2마리 | **-2** (프리팹 인스턴스는 씬에 `PrefabInstance` 블록 1개뿐 — GameObject·컴포넌트는 프리팹 자산 쪽에 있어 안 잡힌다) |
| `StageDirector` 추가 | **+3** (GameObject + Transform + `StageMapGenerator` 컴포넌트) |
| `extractionPoint` 참조용 `stripped` 앵커 | **+1** (Unity 정상 동작 — 프리팹 밖 오브젝트가 다른 프리팹 인스턴스 내부를 참조하면 자동 생성됨. 버그 아님) |

`3+1-2=+2`. **다음부터 이 실측값을 기준으로 삼는다** — 옛 예측치로 비교하면 정상 변경을 오브젝트 유실로 오판한다.

---

## 8. 검증 도구

에디터 창으로 미리보기를 만들면 `.unity` 가 더러워져 머지 규칙과 정면충돌한다. **안 만든다.**

| 도구 | 내용 |
|---|---|
| `StageLayoutTests` (PlayMode) | 아래 6종 |
| 런타임 재생성 키 `#if UNITY_EDITOR` | Play 중 시드 다시 굴려 눈으로 확인 |
| 스크린샷 | 기존 Unity MCP 카메라 캡처 (새 코드 0) |

### 테스트 9종

| # | 이름 | 검사 |
|---|---|---|
| 1 | `Test_StageLayout_Deterministic` | 같은 시드 = 같은 방 목록 (버그 재현 가능성의 근거) |
| 2 | `Test_All_Rooms_Reachable` | 시작 방에서 플러드필하면 모든 방에 닿는다 |
| 3 | `Test_Extraction_Is_On_Floor` | 추출구 좌표가 바닥 칸 — **벽 밖 배치 사고 재발 방지** |
| 4 | `Test_Spawn_Points_On_Floor` | 스폰 후보 전부 바닥 칸이고 벽 인접 아님 |
| 5 | `Test_Home_Biome_Majority` | 홈 바이옴 방 비율 ≥ 60% |
| 6 | `Test_Room_Count_In_Range` | 시드 100개에서 방 개수가 14~20 안 |
| 7 | `Test_Pattern_Never_Blocks_Doors` | 시드 200개에서 기둥이 방 안 통행을 끊지 않는다 (§5-6 보험 검증) |
| 8 | `Test_Safe_Rooms_Guaranteed` | 시드 100개 전부 `Safe` 방 ≥ 2개, 시작 방은 항상 `Safe` |
| 9 | `Test_Start_Room_Is_Home_Biome` | 시드 200개 전부 시작 방이 씬의 홈 바이옴 (실측으로 잡은 버그, 위 §5-6 참고) |

> **테스트 러너에 안 뜨는 문제**: `Assets/Tests/PlayMode` 에 asmdef 가 없다.
> `Game.Gameplay.asmdef` 는 이미 있으므로 테스트용 asmdef 하나만 추가하면 뜬다.
> (`playModeTestRunnerEnabled` 플래그를 켜는 쪽은 안 쓴다 — nunit 이 WebGL 빌드에 섞인다)

---

## 9. 함정 · 부채

| 함정 | 대응 |
|---|---|
| `FogOfWarReveal` 은 **타일이 있는 칸만** 걷어낸다 | 생성기가 `FogOfWar` 층도 반드시 채운다. 안 채우면 **에러 없이 안개가 통째로 사라진다** |
| **벽 오브젝트 이름이 씬마다 다르다** — `Biome`·`BiomeAshfall` 은 `Walls`, `BiomeMarsh` 는 **`Collision`**(늪지는 타일 이름만 다른 게 아니라 벽 레이어 이름 자체가 다르다. 실측으로 잡음) | `StageMapGenerator` 는 인스펙터 참조로만 받으므로 영향 없음. 씬 배선할 때 이름 하드코딩 금지 |
| ~~`Biome`·`BiomeAshfall` 씬은 손배치 시절 안개가 안 떴다~~(해결) — 옛 `FogOfWarReveal.fogLevels` 5칸이 null 참조였다. 늪지만 안개 그림이 있고 나머지 두 바이옴은 애초에 그림이 없었다 | `StageMapGenerator` 가 생성 시점에 `StagePalette.fogLevels` 로 직접 채운다. 세 팔레트 다 늪지 스프라이트를 재사용하되 색만 틴트(§9-2) — 손배치 잔재 문제 자체가 없어짐 |
| **`Tile_GroundBlend_*` 를 벽에 재사용하면 콜라이더가 없다** — 원본은 바닥용이라 `m_ColliderType: None`. 그림은 막힌 듯 보여도 실제로는 통과된다(사용자 실측 스크린샷으로 발견, §9-1) | 벽 전용으로 콜라이더 타입만 `Sprite` 인 복제본 16장×3바이옴을 따로 만든다(`wangBlendCollidable`). 같은 애셋을 바닥·벽 양쪽에 그대로 쓰면 재현된다 — 새 wang 세트를 벽에 얹을 땐 항상 이 함정부터 확인 |
| 늪지만 타일 이름이 다름(`MarshTile_*`) | 팔레트 SO 의 인스펙터 참조. **이름 하드코딩 금지** |
| 타일 PPU | 셀 64 = PPU 64 = 1타일 1유닛. 깨면 지도가 2×2로 어긋난다 |
| `SetTile` 만 개별 호출 | 10,800칸이면 눈에 띄게 느리다. **`SetTilesBlock` 사용** |
| 추출구가 벽 밖 (세 씬 전부 겪은 사고) | 생성기가 방 중심에만 놓는다 + 테스트 3번 |
| **`Biome.unity` 의 `Walls` 오브젝트가 원래 비활성 상태였다**(이번 작업 전부터, 원인 불명) — 켜져 있어야 할 콜라이더가 꺼져 있었다는 뜻 | 배선하며 `SetActive(true)` 로 켰다. `BiomeMarsh`·`BiomeAshfall` 도 4단계(씬 배선)에서 같이 확인할 것 |
| 카메라가 지도 밖 어둠을 봄 | 벽 밴드 3칸 + 배경 검정 (§5-3) |
| Play 중 MCP `RunCommand` 반복 → `static` 소실 | 긴 검증은 호출 수를 줄이고, `Instance` 가 null 이면 Stop→Play |

### 9-1. 벽 = GroundBlend wang 코너 블렌드 (2026-08-08, 부채 청산)

**계획했던 8방향 스톤 타일(`WallTileSet`, N/S/E/W+모서리 개별 그림)은 만들었다가 버렸다.**
사용자가 "돌벽 대신 최신 나무 그림으로, `GroundBlend` 를 써서" 라고 방향을 바꿔서다. 최종 구조가
더 간단해졌다 — 팔레트당 필드 9개(바닥 1 + 벽 8)가 **16장짜리 배열 하나**로 줄었다.

**무엇을 썼나**: `Assets/Art/TileSheets/...tree with lush foliage.png`(256×256, 4×4 wang 시트)가
이미 `Tools/apply_tile_sheets.py` 의 매핑 표(`"lush grassy field"` 접두사 → `biome` 폴더)에
잡혀 있었다 — 예전에 바닥 블렌드용으로 준비해 둔 자산이었다. 스크립트가 "시트 4장이 정확히 다
있어야 실행" 하는 조건이라(archive 폴더에 이미 3장이 남아있어 걸림) 조건을 완화하고 돌렸다.

**바닥과 벽이 같은 자원을 공유한다**: `StagePalette.wangBlend[16]` — `[0]` = 완전 open(그대로 바닥),
`[15]` = 완전 막힘. 바이옴마다 15가 다른 지형이라 "막는 방식" 이 자연스럽게 갈린다(초원=나무숲,
늪지=진흙탕, 화산재=이미 인덱스가 뒤집혀 있어 `[15]`=용암). **새 그림을 하나도 안 만들었다** — 각
바이옴이 이미 갖고 있던 GroundBlend 세트를 벽에도 재사용했을 뿐이다.

**규칙**: 벽 칸 `(x,y)` 의 네 코너 상태를 대각선 이웃으로 판정한다 — `NW = !IsFloor(x-1,y+1)`
(그 대각 이웃이 바닥이 아니면 그 코너는 "막힘"), 나머지 세 코너도 같은 방식. CLAUDE.md 의 바닥 wang
공식을 그대로 재사용: `index = NW*8 + NE*4 + SW*2 + SE*1`. 방 경계와 맞닿은 안쪽 첫 줄은 코너
일부만 막혀 자연스러운 블렌드가 나오고, 밴드 안쪽 2·3번째 줄(사방이 벽)은 `[15]` 로 덮인다.

**처음 짰던 8방향 스톤 규칙은 옛 손배치 씬의 실제 배치 데이터로 검증했었다**(커밋 `ed1c928`
이전의 `Biome.unity` 를 까서 `m_TileIndex`→GUID 매핑을 직접 읽음) — 그림만 보고 방향을 짐작하는
방식은 못 믿는다(늪지 GIF 매핑 사고 전례, CLAUDE.md 애니메이션 절 참고). 결과적으로 그 8방향
시스템은 안 쓰게 됐지만, "정답 소스는 실제 배치 데이터" 라는 방법론 자체는 유효했다.

Play 실측(3씬): 콘솔 에러 0, 코너가 둥글게 블렌드됨, 기둥·복도 연결부 다 자연스러움.
`StageLayoutTests` 200시드 회귀 없음(계산 로직은 안 건드렸으므로 당연하지만 확인함).

**실전 버그: 벽 콜라이더가 없었다(2026-08-08, 사용자 실측 스크린샷으로 발견).** 화면으로는
막힌 듯 보이는데 실제로는 플레이어가 그대로 통과했다. 원인은 `Tile_GroundBlend_*` 가 원래
**바닥용**이라 `m_ColliderType: 0`(None) 이었던 것 — 벽에도 같은 애셋을 그대로 재사용하면서
그림만 막히고 충돌은 안 생겼다. 그 애셋 자체의 콜라이더 타입을 바꾸면 바닥까지 막혀버리므로
(같은 파일을 `Ground`/`Walls` 양쪽이 공유), **벽 전용으로 그림·색은 동일하고 콜라이더 타입만
`Sprite` 인 16장을 따로 만들었다**(`Tile_GroundBlend_{i}_Wall.asset`, 3바이옴×16=48장).
`StagePalette.wangBlendCollidable[16]` 신설, `WallTileAt` 이 이걸 참조하도록 수정.

검증은 스크린샷이 아니라 **`Physics2D.OverlapPoint` 로 벽 칸 전수 조사**했다(§9-3 참고 —
캡처 도구를 이 세션에서 못 믿게 됐다). Biome 4016칸 중 4002칸(99.65%) 확인, Marsh 3910칸 중
3894칸(99.6%) 확인. 나머지 0.35~0.4% 는 전부 "8방향 이웃 전부 벽" 인 완전 고립 칸 —
CompositeCollider2D 폴리곤 접합부의 부동소수점 경계 문제로 셀 중심점이 안 잡힌 것으로 보이며,
사방이 막혀 애초에 플레이어가 도달할 수 없는 자리라 게임플레이에 영향 없다.

**후속: 콜라이더 타입을 `Sprite` 에서 `Grid` 로 다시 바꿨다(2026-08-08, 사용자 스크린샷으로
발견).** `Sprite` 타입은 그림의 알파(잎 윤곽) 를 그대로 콜라이더로 딴다 — 이 wang 그림은 원래
"바닥 블렌드" 용이라 칸마다 잎이 둥글게 덜 채워진 데도 있어서, 콜라이더가 그 모양대로
울퉁불퉁해졌다. `IsWall` 데이터는 칸 단위로 딱 떨어지는데 실제 막힘은 그림 모양 따라
들쭉날쭉해서 "칸 중심은 막혔는데 가장자리는 뚫린 것처럼 보이는" 위화감이 났다. `Grid` 로
바꾸면 그림이 어떻게 생겼든 콜라이더는 항상 칸 전체(정사각형)를 막는다 — 벽은 "완전히
막힘" 이 게임 규칙이므로 그림의 organic 한 모양과 충돌 범위를 분리하는 게 맞다. 재검증은
칸 중심뿐 아니라 **네 가장자리 근처(±0.4유닛)도 함께** 찔러봤다 — 19,695개 검사점 중 19개
(0.48% 칸)만 안 잡혔고 전과 같은 고립 칸 패턴이라 정상.

**후속 2: 방과 직교로 맞닿은 칸은 무조건 완전 막힘 그림으로 고정했다(2026-08-08, 사용자
스크린샷 — "캐릭터가 벽에 밀착을 못 한다").** `Grid` 콜라이더로 칸 전체를 막아도, 코너
블렌드 인덱스 계산이 여전히 대각선 이웃만 보다 보니 방 경계에 바로 붙은 첫 줄(직선 벽
대부분)이 "절반은 잔디, 절반은 나무" 인 블렌드 그림을 썼다 — 콜라이더는 칸 전체를 막는데
그림은 잔디로 보이니 "밟을 수 있을 것 같은데 안 밟히는" 착시가 났다. 고침: `WallTileAt` 이
먼저 직교(상하좌우) 이웃에 바닥이 있는지 본다 — 있으면 대각선 계산 없이 바로
`wangBlendCollidable[15]`(완전 막힘)를 쓴다. 대각선으로만 방과 맞닿은 **진짜 바깥 모서리
칸(방마다 정확히 4개)만** 여전히 부드러운 블렌드를 쓴다 — 방 모서리는 계단식으로 둥글게
깎이고, 그 사이 직선 구간은 그림과 충돌이 처음부터 정확히 일치한다. 확대 스크린샷으로
직선 구간(첫 줄부터 빽빽함)과 모서리(계단식 블렌드 유지) 둘 다 확인. 벽 콜라이더 회귀
검사도 재확인(3833칸 중 19개, 0.5% — 여전히 고립 칸 패턴).

### 9-2. 안개는 AI 생성 대신 색 틴트로 (2026-08-08)

`Biome`·`Ashfall` 전용 안개 그림을 Unity AI 에셋 생성 도구로 새로 그리려 했는데 백엔드
서버(`generators.ai.unity.com`) 가 두 모델(`gemini-3.1-flash`, `gpt-image-1`) 전부에서
`"Received 'Unknown' from server"` 로 실패했다 — 프롬프트·모델 문제가 아니라 서비스 장애.
CLAUDE.md 원칙대로 두 번 진단하고 멈춰서 사용자에게 대안을 물었다.

**새 그림 생성 없이 색만 틴트하는 쪽으로 처리**: 늪지 안개 스프라이트(`FogCloud_A0..A4`)를
그대로 참조하되 `Tile.m_Color` 만 바이옴별로 다르게 곱연산 — 초원은 연두(`0.82,1,0.82`),
화산재는 주황(`1,0.72,0.55`). 새 `Tile_FogCloud_Biome_A{i}.asset`/`Tile_FogCloud_Ashfall_A{i}.asset`
10장을 만들어 팔레트에 배선. 마쉬는 원본 그대로. §9 부채의 "안개 전용 그림" 항목은 이걸로
갈음한다 — 진짜 전용 그림(모양 자체가 다른)은 여전히 미룸.

### 9-3. 스크린샷 캡처 도구를 이 세션에서 못 믿게 됨 (2026-08-08)

안개 레이어(§9-2)를 스크린샷으로 검증하려 했을 때, 데이터·렌더러 설정(sprite/color/레이어/
컬링마스크/isVisible/bounds)이 전부 정상인데도 `Unity_SceneView_Capture2DScene` 캡처에는
전혀 안 보였다. 진단으로 안개 타일맵 색을 완전 불투명 빨강으로 강제해도 그대로 안 보여서,
"내 배선이 틀렸다" 가 아니라 "이 캡처 도구가 특정 레이어를 못 잡는다" 로 결론지었다.
**이후로는 화면 대신 `Physics2D.OverlapPoint`/`GetTile` 같은 코드 레벨 검증을 우선한다** —
이번 벽 콜라이더 버그도 스크린샷이 아니라 사용자가 실제 게임 화면을 캡처해서 보내준 뒤에야
드러났다. MCP 캡처 도구로 "괜찮아 보인다" 는 안심 근거가 못 된다.

### 9-4. 구역 경계 디더링 (2026-08-08, 부채 청산)

사용자가 실측 스크린샷으로 지적한 두 번째 문제 — 초원 나무 경계와 화산재 용암 경계가 전이
없이 화면 한가운데서 뚝 끊기는 것. 새 그림(그라데이션 전용 타일) 없이, **방 밖(복도·벽 밴드)
칸만 확률적으로 두 팔레트를 섞어 찍는 디더링**으로 해결했다.

**방 안은 절대 안 섞는다** — `PaletteAt` 이 먼저 `room.bounds.Contains(cell)` 로 방 안인지
검사해 그 방의 확정 팔레트를 바로 돌려준다. 안 섞는 이유: `SlimeSpawner`·`StageRegionTrigger`
가 읽는 `room.biomeId` 는 방 하나에 값 하나다 — 바닥 그림이 방 안에서까지 섞이면 "여기는
늪지인데 눈엔 절반이 화산재" 처럼 로직과 그림이 어긋난다.

**방 밖(복도·벽 밴드)만 디더링**: 그 칸에서 가장 가까운 방과 두 번째로 가까운 방까지의 거리
차가 `regionBlendRadius`(기본 5칸) 안이면, 그 차이에 비례한 확률로 둘 중 하나의 팔레트를
고른다 — 경계 한가운데(거리차 0)는 50:50, 한쪽으로 멀어질수록 그쪽 확률이 1.0 으로 수렴한다.
동전 던지듯 매 프레임 다르면 안 되므로 `CellNoise(x,y,seed)` 라는 상태 없는 해시로 고정한다
— 같은 칸은 같은 시드에서 항상 같은 결과가 나와야 재생성(§5-7 `useFixedSeed`)이 의미가 있다.

**`StageLayout.BiomeIdAt` 은 안 건드렸다** — 그 함수는 방 트리거(`StageRegionTrigger`)가 읽는
게임플레이용 API 라 여전히 "가장 가까운 방 하나" 라는 단순한 규칙을 유지한다. 디더링은
`StageMapGenerator.PaletteAt` 안에서만 일어나는 **그리기 전용** 결정이라 게임 로직에 영향이
없다 — `StageLayout` 을 순수 계산 레이어로 두겠다는 원래 설계(§4) 그대로다.

실측(에디트 모드에서 `Generate()` 직접 호출 — Play 가 이 세션에서 여러 번 멈춰서 물리 검증만
Play 로, 그림 검증은 에디트 모드로 나눠 확인했다): 시드 하나를 전체 스캔해서 "5칸 반경 안에
두 바이옴이 같이 나오는 칸" 38개 확인 — 하드 컷이면 0이어야 하니 디더링이 실제로 작동한다는
뜻이다. 벽 콜라이더도 같은 시드에서 재검사해 회귀 없음 확인(0.31% 고립 칸, §9-1 과 같은 패턴).

### 9-5. 장식 배치 + 방 실루엣 다양화 (2026-08-08)

맵 전경을 놓고 본 문제 5가지 중 3가지를 고쳤다.

| # | 문제 | 처리 |
|---|---|---|
| 1 | **장식이 0개** — `Decor` 층을 생성기가 아예 안 건드려 자산 10종이 통째로 놀고 있었다 | `PaintDecor()` 신설 |
| 2 | **옛 손배치 장식 23개가 유령으로 남음** — 24×16 시절 좌표라 지금 지도의 벽 속에 박혀 있었다(`ClearAllTiles` 대상에서 빠져 있었음) | `Generate()` 가 `decorTilemap` 도 지운다 |
| 3 | **모든 방이 완벽한 직사각형** — 방 20개 실루엣이 전부 같아 "아까 그 방인가?" 구분 불가 | `NotchCorners()` 신설 |
| 4 | 바닥이 완전 균일(wang 16장 중 `[0]` 만 씀) | 미룸 — §9-4 디더링과 겹침 |
| 5 | 바이옴이 덩어리로 뭉침 | 미룸 — §9-4 에서 경계만 완화함 |

**장식 배치 규칙** (`StagePalette.decorTiles`, 전부 콜라이더 None 이라 길을 안 막는다):

- **복도엔 안 놓는다.** 복도는 폭 3칸뿐이라 장식이 끼면 길인지 막힌 곳인지 헷갈린다 —
  안개로 시야가 좁은 지도에서는 치명적이다. *방=머무는 곳, 복도=지나가는 곳* 구분을
  그림으로도 유지한다.
- **벽에 붙은 칸은 3배 자주.** 자연에서 바위·풀은 가장자리에 몰리고, 방 한가운데는 비어
  있어야 싸울 자리가 된다. 균일하게 뿌리면 전투 공간이 지저분해진다.
- **시작 방 ×0.5 / 둥지 ×1.5.** 첫 화면은 조작을 배우기 좋게 깔끔히, 둥지는 어질러
  "여긴 뭔가 산다" 를 그림으로 먼저 알린다.
- 스폰·추출구 반경 3칸은 비운다(표식을 가리면 안 된다).
- 시드 고정(`CellNoise`)이라 같은 지도는 장식까지 똑같이 재현된다.

바이옴별 자산: 초원 2종(꽃·바위) / 늪지 4종(갈대·연꽃·통나무·바위) / 화산재 2종(잉걸불·그루터기).
실측: 바닥 5,149칸 중 장식 426개(8.3%), **벽·빈칸 위 오배치 0**.

**모서리 깎기**(`NotchCorners`): 네 모서리를 각각 50% 확률로 삼각형으로 깎는다 — 16가지
실루엣이 나온다. 깊이는 방 짧은 변의 1/4(2~5칸)로 제한한다. 작은 방에서 크게 깎으면 남는
바닥이 마름모가 되어 싸울 자리가 사라진다.

**복도보다 먼저 깎는 것이 핵심이다.** 복도는 그 뒤에 `FillFloor` 로 뚫리므로, 깎아낸 자리를
지나가야 하면 알아서 다시 메운다 — 연결성이 저절로 보장된다. 순서를 뒤집으면(복도 먼저,
깎기 나중) 길이 끊긴다. 실측: 시드 200개에서 `unreachableRooms=0` `splitRooms=0`
`extractionBad=0`, 방 97.5%가 실루엣이 갈렸다.

### 9-6. "배경이 인위적" 해결 — 노이즈 침식 + 셀룰러 오토마타 (2026-08-08)

사용자가 오픈월드 생성 아키텍처 문서(`Unity_2D_Procedural_Map_Generation_Architecture`)를
가져온 진짜 의도는 아키텍처 교체가 아니라 **"배경이 너무 인위적으로 보인다"** 였다. 문서의
오픈월드 인프라(청크 스트리밍·랜드마크 그래프·A* 도로)는 이 프로젝트 규모에 여전히 안 맞지만
— 스테이지 한 판이 문서가 말하는 청크 하나(128×128)보다 작고 생성이 3ms 다 — **생성 기법
3가지는 규모와 무관하게 유효**해서 그것만 골라 넣었다.

| 인위적으로 보인 원인 | 문서 기법 | 적용 |
|---|---|---|
| **경계가 완벽한 수평/수직 직선**(압도적 1위) | §10 노이즈 | 방 가장자리 노이즈 침식 |
| 침식만 하면 가장자리가 소금 뿌린 듯 자글거림 | §18 셀룰러 오토마타 | 이웃 수로 돌기는 깎고 흠집은 메움(2패스) |
| 벽 밴드 두께가 균일하게 3칸 | — | 덩어리 노이즈로 3~5칸 변동 |
| 복도가 완벽한 직선 L자, 폭 고정 | — | 직각 방향 사행 + 폭 2~3 변동 |

**칸 단위로만 깎는다.** 그림의 알파로 깎으면 §9-1 의 "밟힐 것 같은데 안 밟힘" 이 재발한다 —
셀 단위라 콜라이더가 보이는 그림과 정확히 일치한다.

**복도는 침식하지 않는다.** 폭 3이 2나 1로 좁아지면 길이 막힌다. 방 안쪽만 깎고, 복도와
맞닿은 칸(문)은 `TouchesCorridor` 로 지킨다.

**연결성 3중 방어**(이 기법의 알려진 함정이 "갈 수 없는 웅덩이" 다):

1. 침식 후 모든 방이 도달 가능한지 검사 — 아니면 그 시드는 침식을 **통째로 되돌린다**
   (생성이 3ms 라 되돌려도 사실상 공짜).
2. `PruneDisconnectedFloor()` — 시작점에서 못 닿는 바닥은 전부 벽으로 되돌린다.
   **실측으로 필요성이 드러났다**: 이 패스 없이는 시드 200개에서 방 81개에 본체와 끊긴
   바닥 조각이 남았고, 거기 스폰된 슬라임은 영영 못 잡는다.
3. 복도 꺾이는 지점은 사행 없이 통째로 메운다 — 두 구간이 서로 다른 축으로 흔들려
   이음매에서 한 칸만 겹치거나 끊길 수 있다.

**사행이 길을 안 끊는 이유**: 한 걸음이 1칸이고 흔들림도 한 걸음에 최대 1칸으로 묶여 있어
(`Mathf.Clamp(raw, prev-1, prev+1)`) 연속한 두 걸음이 칠하는 사각형이 반드시 겹친다.

**밴드 지터는 두껍게만 건다**(0 ~ +2). 얇아지면 §5-3 의 "3칸" 보장이 깨져 플레이어가 벽에
붙었을 때 카메라가 지도 밖 빈 공간을 본다. 처음엔 ±2 로 넣었다가 실측 스크린샷에서 공백이
크게 노출돼 고쳤다.

**스폰 후보 조건도 같이 완화했다**: 예전엔 "8이웃 전부 바닥" 을 요구했는데 침식으로 경계가
들쭉날쭉해지자 후보가 방당 1칸까지 떨어졌다. 대각선이 벽이어도 슬라임은 끼지 않으므로
직교 4이웃만 본다 — 최소 4칸 / 평균 175칸으로 회복.

실측(시드 200개): `unreachableRooms=0` `splitRooms=0` `extractionBad=0` `startNotHome=0`,
생성 **6.8ms/장**(예전 2.7ms — 침식·CA·연결성 패스가 늘었지만 60fps 예산 16ms 안).
Play 실측: 벽 구멍 4,935칸 중 5칸(0.10%, 예전 0.48%보다 오히려 개선), 슬라임 62마리 전원
정상 위치.

### 9-7. 나무 프롭 배관 — Y 정렬 + Props 층 (2026-08-08, 자산 대기)

사용자가 이상적 레퍼런스(탑다운 픽셀아트 숲)를 제시. 격차를 짚어 보면 **자산 형식 차이**라
코드로 못 메운다:

| | 지금 | 레퍼런스 |
|---|---|---|
| 벽 표현 | 평면 캐노피 텍스처를 격자로 반복 | **나무 한 그루씩이 개별 오브젝트** |
| 시점 | 완전 수직 부감 | **3/4 부감** — 줄기 정면이 보임 |
| 깊이 | 없음 | 나무 뒤로 캐릭터가 지나감 |

wang 중간 인덱스로 경계를 부드럽게 하는 길은 막혀 있다 — 반쯤 잔디인 칸이 막혀서 §9-1 의
"밟힐 것 같은데 안 밟힘" 이 재발한다. 그래서 **프롭 방식으로 갈 수밖에 없다.**

자산이 오기 전에 배관만 먼저 깔았다(자산 오면 꽂기만 하면 되게):

1. **프로젝트 Y 정렬**: `GraphicsSettings.transparencySortMode = CustomAxis`, 축 `(0,1,0)`.
   Y 가 낮을수록 앞에 그려진다.
   - **기존 스프라이트가 안 뒤집히는 것을 먼저 확인했다.** HP바(order 200/201)·약화 표식(202)·
     경계 표식(10)·그림자(−1)가 전부 본체(0)와 다른 `sortingOrder` 를 갖고 있고, order 는
     정렬축보다 **먼저** 비교되므로 상대 순서가 유지된다. order 0 인 것은 플레이어·슬라임·
     추출구 본체뿐 — 정확히 Y 정렬이 필요한 대상이다.
2. **`Props` 타일맵 신설**(3씬): `mode = Individual`, `sortingOrder = 0`.
   - `Chunk` 모드면 덩어리 하나로 묶여 Y 정렬이 안 먹는다 — 타일마다 따로 정렬돼야 한다.
   - 플레이어와 같은 order 라 둘 사이 앞뒤가 Y 로 갈린다.
3. `StagePalette.treeProps` + `StageMapGenerator.PaintTreeProps()` — 벽 덩어리 위에 나무를
   흩는다. **바깥 테두리 우선**(75%), 안쪽은 성기게(35%) — 캐노피가 걸어다니는 쪽으로 넘어와야
   숲 윤곽이 흐려진다. 프롭 사이 최소 간격 `treeSpacing`(3칸).
   - **충돌은 프롭이 아니라 벽 칸이 계속 담당한다** — 그림과 충돌이 정확히 일치하는 원칙 유지.
   - `treeProps` 가 비면 조용히 넘어가 지금 모습 그대로다.

임시로 기존 캐노피 타일을 프롭으로 꽂아 배관을 검증했다(367개 배치, Individual, CustomAxis
확인) 후 되돌림.

**요청한 자산**: 3/4 부감 나무 프롭 — 소 128×128(2×2칸) / 중 192×192(3×3) / 대 256×256(4×4),
크기별 3종. 피벗은 **줄기 바닥 중앙**, 캐노피는 그 위·좌우로 뻗음, 투명 배경.
덤불 채움 타일 64×64 3~4종. (선택) 흙길 wang 16종.
스타일은 **나무만 레퍼런스 수준**으로 올리고 캐릭터는 유지하기로 결정.

### 9-8. 나무·덤불 자산 반영 (2026-08-08)

사용자가 나무 3종(128×128 ×2, 256×256 ×1) + 덤불 4종(64×64) 제공. 정확히 요청한 스펙대로
왔다(중간 크기 192 는 안 왔지만 소·대만으로 충분히 진행).

**피벗은 눈짐작이 아니라 알파 경계 실측으로 잡았다**(CLAUDE.md 의 "그림을 봐라" 원칙과 같은
이유 — 실측이 아니면 발치가 어긋나 나무가 공중에 뜨거나 땅에 파묻힌 것처럼 보인다):

```python
# 알파>15 인 픽셀의 바운딩 박스, 하단부 20% 구간에서 뿌리 폭 좌우 범위
```

| 자산 | 캔버스 | 알파 하단 여백 | 피벗(정규화) |
|---|---|---|---|
| Tree_Round_128 | 128×128 | 8px | (0.4922, 0.0703) |
| Tree_Gnarled_128 | 128×128 | 8px | (0.4688, 0.0703) |
| Tree_Ancient_256 | 256×256 | 10px | (0.5059, 0.0430) |

임포트 설정: `Point` 필터(픽셀아트 유지), `Single`, **PPU 64**(128px 그림이 정확히 2×2칸,
256px 가 정확히 4×4칸 — 별도 스케일 계산 없이 요청 사양 그대로 맞아떨어졌다), `Custom` 피벗.

**Tile 콜라이더는 전부 `None`.** 나무는 순수 시각 — 충돌은 여전히 벽 칸(`wangBlendCollidable`,
Grid)이 담당한다. §9-1 에서 확립한 "그림과 충돌은 별개 레이어" 원칙을 그대로 지켰다.

**덤불은 별도 채움 레이어를 안 만들고 기존 `decorTiles` 에 얹었다.** 원래 §9-7 계획은
"벽 내부를 덤불 채움 타일로" 였는데, 벽 칸엔 이미 wangBlend 캐노피 텍스처가 빈틈없이 깔려
있어 추가 채움이 불필요했다 — 대신 덤불을 방 장식(`decorTiles`)에 꽃·바위와 함께 넣으니
`PaintDecor()` 의 기존 "벽에 붙은 칸 3배 확률" 규칙이 그대로 적용돼 벽 근처에 자연스럽게
몰린다. 새 로직 없이 기존 배관을 재사용했다(ponytail).

Biome 팔레트만 배선했다(레퍼런스가 초원 숲이었으므로) — **늪지·화산재는 나무가 없다.**
자기 색(진흙탕/용암)의 wang 캐노피만 유지되고 프롭은 안 얹힌다. 그 두 바이옴 나무는 부채로
남긴다.

실측: 벽 콜라이더 5283칸 중 4개(0.08%, 여전히 고립 칸 패턴 — 나무 도입으로 회귀 없음),
나무 프롭 391개 배치. **플레이어를 나무 발치 앞에 세워 스크린샷으로 Y 정렬 확인** — 캐노피가
플레이어 위로 그려짐(§9-7 에서 깐 배관이 실제로 작동).

### 9-9. 벽 텍스처를 완전히 없앰 — 콜라이더만 남기고 데코 레이어가 경계를 대신함 (2026-08-08)

§9-8 직후 사용자가 스크린샷으로 지적: 진짜 바깥 모서리 칸(방마다 4개)이 여전히
`wangBlendCollidable[15]`(캐노피 블렌드 그림)를 썼는데, 바닥과 바로 붙어 있어 "바닥에
나무가 자란 것처럼 부자연스럽다" — 그리고 뒤이어 "무늬가 시선을 강탈한다", 마지막으로
"타일에 벽을 만들지 말고 데코로 레이어를 세워서" 로 요구가 좁혀졌다. 세 요청이 가리키는
결론은 하나: **벽 밴드 자체엔 아무 그림도 없어야 한다.**

**최종 구조**: `WallTileAt` 이 코너 계산 없이 `PaletteAt(x,y).wallShadow` 하나만 반환한다.
그 타일은 `color.a = 0`(완전 투명) + `colliderType = Grid` — 그림은 하나도 안 그리고
충돌만 칸 전체에 남긴다. 벽 밴드가 어떻게 보이는지는 전적으로 **Props 층(나무·펜스)**
이 결정한다 — 프롭이 없는 빈 벽 칸은 카메라 배경(Skybox, 짙은 남색)이 그대로 드러나
"그 뒤가 안 보인다" 는 원래 요청을 그림 한 장 없이 만족한다.

**시행착오**: 처음엔 `wangBlendCollidable[15]` 를 40% 밝기로 어둡게 틴트한 "그림자 타일"을
만들었다(§9-9 원래 버전) — 진짜 모서리 체크무늬는 없앴지만, 어둡게 해도 원본 텍스처의
크로스해치/소용돌이 무늬 대비가 그대로 남아 "여전히 시선을 강탈한다" 는 지적을 받았다.
그다음 "에셋 안에 벽 모양이 있을 것" 이라는 힌트로 옛 8방향 돌벽(§9-1 에서 버렸던 자산)을
재검토했지만, "타일에 벽을 만들지 말라" 는 후속 요청으로 타일 텍스처 자체를 쓰는 접근을
완전히 접었다 — 최종적으로 투명 타일 + 프롭으로 정리됐다.

**펜스 프롭 추가**: `Assets/Resources/Props/hub_decor/Structure_Fence.png`(원래 Hub 전용)를
발치 피벗 실측 후 초원 `treeProps` 에 나무 3종과 함께 얹었다 — 나무와 같은 배치 로직
(`PaintTreeProps`, 바깥 테두리 우선·간격 유지)을 그대로 재사용해서 새 코드 없이 프롭 종류만
늘렸다.

**검증**: `Grid/Walls`(Biome·Ashfall) / `Grid/Collision`(Marsh, 이름이 다름 — §9 기존 함정)
양쪽에서 벽 칸 타일이 `sprite=Invisible, color.a=0, colliderType=Grid` 인 것을 데이터로
확인. 물리 회귀도 재검사(0.12%, §9-1 이후 패턴과 동일한 고립 칸 수준 유지) — 그림을
지워도 충돌은 그대로다.

**스크린샷 캡처 도구가 또 한 번 오해를 만들 뻔했다**: Scene 뷰 캡처가 벽 바깥을 흰색으로
보여줘서 "카메라 배경이 흰색인가?" 헷갈렸는데, 실제 `Camera.main.backgroundColor` 를
직접 조회하니 Skybox 짙은 남색이었다 — §9-3 에서 이미 결론 낸 대로 이 캡처 도구는 특정
레이어(이번엔 카메라 배경)를 못 믿을 근거로 다시 확인됐다. 늪지(Marsh) 스크린샷에서 본
"짙은 크로스해치" 도 처음엔 벽이 안 지워진 걸로 의심했으나, 벽 칸 데이터를 직접 조회해
투명함을 확인했고 — 실제로는 **안개가 덮인 바닥**이었다(무관한 정상 동작). 데이터 검증
없이 스크린샷만 보고 판단했으면 존재하지 않는 버그를 쫓았을 것이다.

**남은 부채**: 늪지·화산재엔 아직 프롭이 없다(treeProps 가 biome 팔레트에만 배선됨,
§9-8 에서 이미 기록) — 그 두 바이옴은 지금 벽이 완전히 빈 채로(카메라 배경만) 보인다.
고사목·그을린 나무 자산이 오면 같은 배관에 꽂기만 하면 된다.

### `ponytail:` 부채 — 의도적으로 미룬 것

| 미룬 것 | 지금 대신 | 언제 갚나 |
|---|---|---|
| ~~벽 8방향 모서리~~ | **완료(2026-08-08)** — 아래 §9-1 | — |
| ~~구역 경계 그라데이션~~ | **완료(2026-08-08)** — 아래 §9-4 | — |
| ~~`Decor` 층 장식~~ | **완료(2026-08-08)** — 아래 §9-5 | — |
| 기둥 전용 그림 | **벽과 같은 wang 코너 블렌드 재사용** | §9-1 로 자동 해결 — 기둥도 `IsWall` 데이터라 같은 `WallTileAt` 를 탄다 |
| `Biome`·`Ashfall` 전용 안개 그림 | **늪지 스프라이트 재사용 + 색 틴트**(§9-2) | AI 생성 서버 복구되면 진짜 전용 그림으로 |
| 방 패턴 `Choke`·`Arena`·`Bridge` | v1 은 `Pillar`/`Safe`/`Nest` 3종 | `switch` 에 `case` 한 줄씩. `Bridge` 는 늪지 `Tile_MarshVoidWall` 이 이미 있어 늪지부터 가능 |
| 바이옴 선택 UI (Hub) | 진입점 1개 유지 | 주말 계획 §9 타협 1순위 그대로 |
| 늪지·화산재 프롭 | 벽이 완전히 빈 채(투명, §9-9) | 초원 나무 자산만 받음. 늪지=고사목, 화산재=그을린 나무·바위 필요 |
| 중간 크기(192×192) 나무 | 소·대 2종으로 대체 | 자산 오면 `treeProps` 배열에 추가만 하면 됨 |
| 흙길 wang 16종 | 없음 | 선택 사항, 필요해지면 |

---

## 10. 작업 순서

| # | 작업 | 끝났다고 볼 조건 |
|---|---|---|
| 1 | `StageLayout`(방·복도·구역) + 테스트 1~6 | ✅ 시드 200개 통과 (Unity 씬 없이) |
| 1.5 | 방 패턴 3종 + 테스트 7~8 | ✅ 시드 200개에서 문 안 막힘. 실측으로 버그 2건 잡음(추출구 벽 매몰, 방 안 고립 웅덩이) |
| 2 | `StagePalette` SO + asset 3개 | ✅ 세 팔레트 전부 floor/wall/fog 5종 완전 배선 확인 |
| 3 | `StageMapGenerator` | ✅ `Biome.unity` 에 `StageDirector` 배선 후 Play 실측. 바닥/벽/안개 데이터 100% 일치(3917/3917 벽 칸), 기둥·안개 리빌·추출구 배치 전부 시각 확인 |
| 4 | 씬 3개 배선(손배치 삭제 + `StageDirector`) | ✅ 3씬 전부 Play 실측: 방 16개씩, 콘솔 에러 0. `Collision`(늪지 벽 이름) 함정 잡음. 오브젝트 총수 기준선 갱신(Biome 110/Marsh 395/Ashfall 116) |
| 5 | `SlimeSpawner` | ✅ Play 실측: 50~64마리, 시작 방 0마리, 최소 간격 위반 0, 벽 위 스폰 0, `Nest` 배율(×3) 정상. **실측으로 버그 1건 잡음** — 시작 방이 외지 바이옴으로 나올 수 있었다(§5-4 수정, 테스트 9번 추가) |
| 6 | `StageRegionTrigger` + `GameManager.SetCurrentBiome` | ✅ Play 중 플레이어를 늪지 방으로 텔레포트해 실측 — `CurrentBiomeId` 가 `biome_default`→`biome_marsh` 로 바뀌고 콘솔에 `region_entered biome=biome_marsh` 정확히 찍힘. 콘솔 에러 0 |

**1~3 이 끝나면 "맵이 넓어졌다"는 이미 달성**이다. 4~6 은 그 위의 층.

### 시간 없을 때 버리는 순서

1. `StageRegionTrigger` (구역은 그림만, 낙인은 씬 고정) — 게임은 돈다
2. 거리 등급 페이싱 (패턴을 방마다 균일 확률로 굴림)
3. 추가 연결 3개 (막다른 길 그대로)
4. 안개 채우기 (지도가 다 보이는 상태)

**`Pillar` 는 버리지 않는다.** 코드 5줄인데 직선 추격을 재미로 바꾸는 유일한 조각이다.
`Safe`/`Nest` 는 스포너 숫자일 뿐이라 버릴 것도 없다.
— **끝까지 남길 것**: `StageLayout` + `StageMapGenerator` + `SlimeSpawner`
