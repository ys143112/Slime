# Slime Stigma Ranch — 게임 개요

기획 아이디어를 찾기 위한 요약. 현재 구현된 것만 적는다.
(코드 구조는 `CLAUDE.md`, 원본 기획서는 `Doc/설계/plans/slime-rancher-roguelite.json`)

---

## 한 줄

**로그라이트 다이브 + 몬스터 목장 시뮬레이션.** 위험한 바이옴에 잠수해서 슬라임을
잡아 오고, 안전한 목장에서 교배해 더 강한 슬라임을 만들어 다시 잠수한다.

## 핵심 루프

```
목장(Hub)  ──다이브──▶  바이옴(전투)  ──추출 성공──▶  목장
   ▲                        │
   │                        └──사망──▶ 이번 런에 잡은 것 전부 몰수
   │
   └── 교배 · 휴식 · 동행 선택
```

**긴장의 원천은 "몰수"다.** 다이브 중 잡은 슬라임은 `RunSatchel`(배낭)에만 쌓이고,
추출 지점(`ExtractionPoint`)까지 걸어가 살아서 나와야 `PlayerRoster`(영구 인벤토리)로
넘어간다. 죽으면 그 런의 수확은 전부 사라진다. 목장에 이미 있던 슬라임은 안전하다.

---

## 게임 시스템

### 1. 다이브 / 런 (spec-001)

- 목장의 다이브 트리거를 밟으면 바이옴 씬으로 간다. 바이옴 3종: 초원 / 늪지대 / 화산재.
- 런 종료 사유는 둘: **추출**(성공) / **사망**(몰수).
- 화면에 추출 지점 방향 화살표가 뜬다.
- 모든 런 이벤트를 `RunLogWriter` 가 기록한다.

### 2. 포획 (spec-002)

- 야생 슬라임을 3대 때려 **약화** 상태로 만든 뒤 `E` 로 잡는다.
- 약화 상태에는 "눌려서 검게 타는" 전용 애니메이션이 있다 — 잡을 수 있다는 신호.
- 잡은 것은 배낭행. 추출해야 인벤토리로.

### 3. 교배 (spec-003)

목장의 교배장에서 두 마리를 골라 교배 → 알 → 부화.

- **스탯**: 부모 평균 + 무작위 편차.
- **종(=그림)**: 부모 중 50:50 으로 물려받는다. 12% 확률로 **교배 전용 종**(무지개)이 나온다.
- **종 편향은 벗겼다 다시 씌운다** — 부모의 종 배율을 나눠 원본 스탯을 복원하고, 섞은
  뒤에 자식 종의 배율을 곱한다. 안 그러면 강한 종끼리 교배할수록 배율이 누적된다.

### 4. 낙인 / 오염 티어 (spec-004, 006)

- 같은 바이옴에 반복해서 잠수하면 **낙인 스택**이 쌓인다. 스택 3당 야생 슬라임 티어 +1
  (최대 5). 티어 배율 `1 + 0.15 × tier`.
- 즉 **같은 곳을 파먹을수록 그곳이 위험해진다** — 바이옴을 돌려 쓰게 만드는 압력.
- 오염된 계보를 추적해(`CorruptedGeneTagger`) 조건을 채우면 계보가 진화한다.

### 5. 돌연변이 / 이로치 (spec-005)

- 포획·부화 시 **돌연변이** 굴림. 걸리면 스탯이 반전된다(약한 스탯이 강해지고 반대도).
- 돌연변이 안에서 다시 25% 로 **이로치**(색 변종). 즉 이로치는 항상 돌연변이다.
- 이로치 색은 **바이옴마다 다르다**. 파란 슬라임은 늪지=초록, 용암지=빨강, 초원에선
  이로치가 아예 안 나온다.

### 6. 전투 (spec-007)

| | HP | 공격 | 방어 |
|---|---|---|---|
| 플레이어 | 30 | 8 | — |
| 야생 슬라임 (tier 0) | 20 | 5 | 2 |

- `Space` 근접 공격, **쿨다운 0.5초**(화면 좌하단 게이지). 예전엔 연타로 다 잡혔다.
- 피해 = `공격 - 방어`, 최소 1.
- 야생은 **접촉 피해**를 1.2초마다 준다(충돌 콜백이 아니라 거리 검사 — 물리가 잠들면
  콜백이 안 오기 때문).
- 피해·회복이 뜨는 **떠오르는 숫자**(메이플스토리식). 빨강=피해, 초록=`+회복`.

### 7. 진영

`Faction { Player, Wild }`. 아군 오사 차단은 **`ApplyDamage` 구현부**에 있어 새 공격자가
생겨도 못 우회한다. 진영을 안 밝히는 출처(환경 피해)는 통과시킨다.

### 8. 동행 슬라임 ★ 최근 구현

**인벤토리에서 한 마리를 골라 데리고 다닌다.** 동시에 **한 마리만**.

- 목표점 추종으로 따라온다(거리 임계값이 아니라 위치 오차에 비례한 속도 — 임계값
  방식은 Move↔Idle 이 떨려 애니메이션이 되감겼다).
- 반경 4.5 안의 야생을 스스로 찾아 1.5초마다 때린다.
- 씬을 넘어가도 따라온다(`DontDestroyOnLoad`). 너무 멀어지면 순간이동.
- **죽으면 인벤토리에서 영구 삭제된다.** 화면 좌하단에 동행 체력바.

### 9. 종별 패시브 ★ 최근 구현

네 패시브 전부 "주기마다 반경 안을 훑어 뭔가 한다" 라는 같은 모양. 반경은
**바닥에 원으로 그려진다**(`LineRenderer`, 패시브가 돌 때마다 밝게 번쩍).

| 종 | 패시브 | 반경 | 효과 |
|---|---|---|---|
| Guard | **도발** | 4 | 근처 적이 나를 먼저 노린다. **공격력 0** — 순수 방패 |
| Rainbow | **범위 회복** | 3.5 | 2초마다 +2. **진영을 안 가린다 — 적도 같이 낫는다** |
| Bog | **둔화** | 3 | 반경 안의 적이 느려진다 |
| Lava | **지속 피해** | 2.5 | 1.5초마다 도트 |

### 10. 목장 시설 (spec-010)

- **휴식소**: 슬라임을 넣어 두면 시간이 지나며 회복한다(틱마다 최대 체력의 10%, 완치까지
  약 20초). 다친 슬라임을 굴리려면 기다려야 한다 = 로스터를 넓게 유지할 압력.
- **교배장**: `U` 로 화면 UI 를 연다. 씬에도 이름표를 달아 세워 뒀다.

### 11. 야생 슬라임 AI (spec-012)

플레이어/동행 중 가까운 쪽을 쫓는다. 단, **도발 대상이 있으면 그쪽이 우선**.

---

## 종 5가지

**종 하나가 곧 그림 하나다.** ScriptableObject 가 그림·이로치 규칙·스탯 편향·패시브를
전부 들고 있어 밸런스는 재컴파일 없이 만진다.

| 종 | 등장 | 스탯 편향 | 이로치 |
|---|---|---|---|
| **Blue Slime** (`slime_basic`) | 전 바이옴 | 기본 | 늪지=초록, 용암지=빨강, 초원엔 없음 |
| **Guard Slime** (`slime_guard`) | 초원 | HP 1.4 / **공격 0** / 방어 2.2 / 속도 0.8 | 3색 랜덤 |
| **Rainbow Slime** (`slime_rainbow`) | **교배로만** | 전체 0.8~0.9 | 없음 (단일 개체) |
| **Bog Slime** (`slime_bog`) | 늪지대 | 기본 | 3색 랜덤 |
| **Lava Slime** (`slime_lava`) | 화산재 | 전체 1.1~1.2 | 3색 랜덤 |

---

## 조작

| 키 | 동작 |
|---|---|
| WASD | 이동 |
| Space | 근접 공격 (쿨다운 0.5초) |
| E | 포획 (약화 상태에서만) |
| I | 인벤토리 (동행 선택도 여기서) |
| U | 교배 UI |
| Z / X / C | 휴식소 |

---

## 기술 현황

- Unity 6000.5.5f1, 2D, 새 Input System 전용.
- 기준 해상도 960×540 (16:9), 창 고정. UI 캔버스는 1920×1080 기준 스케일.
- 씬: `Boot`(시작 화면) → `Hub` / `Biome` `BiomeMarsh` `BiomeAshfall`.
- 영속 싱글턴은 전부 `Boot` 에 있다(`GameManager` `PlayerRoster` `BiomeStigmaManager`
  `AudioManager` `InventoryUI` `BreedingUIPanel` …).
- 세이브: 낙인 + 로스터 2개 JSON. WebGL 에서는 `PlayerPrefs` 로 갈린다.
- 애니메이션: 플레이어·슬라임이 **같은** `ActorAnimation` 컴포넌트. idle / 8방향 이동 /
  8방향 공격 / 피격 / 약화. 원본 GIF 을 파이썬으로 펴서 Unity 가 클립을 굽는다.
- 바닥은 16장짜리 wang 타일셋.
- **효과음·배경음 클립이 아직 하나도 없다.** 배선(`AudioManager`)만 있고 전부 무음.
- 테스트: PlayMode 5파일(전투 / 추적 / 교배장 / 배낭 / 종 편향).

---

## 아직 없는 것 (아이디어 붙일 자리)

- **오디오 전부** — 틀만 있고 클립 0개.
- **경제·자원** — 돈, 상점, 아이템, 소모품이 없다. 다이브의 보상이 슬라임뿐이다.
- **플레이어 성장** — 플레이어 스탯이 고정 30/8 이다. 무기·장비·레벨 없음.
- **다이브 안의 구조** — 바이옴이 한 장짜리 고정 맵이다. 방 생성, 층, 보스가 없다.
- **동행 조작** — 데리고 다니기만 하고 명령(공격 지시, 대기, 스킬 발동)이 없다.
- **다중 동행** — 한 마리 제한.
- **종별 스킬** — 패시브 넷뿐. 능동 스킬이 없다.
- **메타 진행** — 런을 넘어 쌓이는 것이 로스터와 낙인뿐이다. 해금·업그레이드 트리 없음.
- **도감·수집 보상** — 이로치·돌연변이를 모을 이유가 게임 안에 없다.
- **UI 다듬기** — 한글/영어가 섞여 있고 폰트 작업이 진행 중.
